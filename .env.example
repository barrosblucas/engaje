DATABASE_URL="postgresql://postgres:postgres@localhost:5432/engaje"

SUPER_ADMIN_EMAIL="admin@bandeirantes.ms.gov.br"
SUPER_ADMIN_PASSWORD="change-me"

SESSION_SECRET="change-me-super-secret-session-key-at-least-32-chars"

# Logs
# Em desenvolvimento, o padrão é `debug`.
# Em produção, o padrão é `info`.
LOG_LEVEL=""

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3200/v1/auth/google/callback"

# App URLs
APP_URL="http://localhost:3100"
# Opcional: lista de origins permitidos no CORS (separados por virgula)
# Exemplo para desenvolvimento em LAN:
# APP_URLS="http://localhost:3100,http://192.168.1.21:3100"
APP_URLS=""
API_URL="http://localhost:3200"
# Frontend (Next.js)
# Em LAN, prefira usar o mesmo host do navegador:
# NEXT_PUBLIC_API_URL="http://192.168.1.21:3200"
# Em producao com API em subdominio dedicado (opcao 1):
# NEXT_PUBLIC_API_URL="https://api.seu-dominio.com"
NEXT_PUBLIC_API_URL="http://localhost:3200"

SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

REDIS_URL="redis://localhost:6379"

# Testes da API (obrigatorio usar banco de teste isolado)
# Crie `.env.test` com DATABASE_URL apontando para um banco dedicado, por exemplo:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/engaje_test"
